# results/hono-node

Committed artifacts for this cell: `metrics.json` and `report.md`, written by
`pnpm benchmark run` and `pnpm benchmark report`.

Empty until the contract suite is green on all four cells. A number produced
before the cells are proven equivalent measures two different programs.

A cell that could not be built or run records `status: "unavailable"` with the
reason, so an absent cell is visible data rather than a silent gap.
