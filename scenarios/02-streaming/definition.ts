import type { ScenarioDefinition } from '../types.js'

/** Real-time token delivery */
export const definition: ScenarioDefinition = {
  id: 'streaming',
  agentId: 'simple-agent',
  measures: 'Real-time token delivery',
  measuredRuns: 250
}
