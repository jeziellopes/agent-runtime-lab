import type { ScenarioDefinition } from '../types.js'

/** Graph execution overhead */
export const definition: ScenarioDefinition = {
  id: 'multi-step-workflow',
  agentId: 'multi-step-agent',
  measures: 'Graph execution overhead',
  measuredRuns: 250
}
