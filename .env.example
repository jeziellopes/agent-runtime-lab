# Identical across all four cells. Copy to .env; never commit the copy.
# Day-to-day development needs no API key at all: LLM_MODE=replay is the default
# for every benchmark run and every test.

LLM_MODE=
LLM_PROVIDER=
MODEL_NAME=
OPENAI_API_KEY=
MAX_ITERATIONS=
MAX_RETRIES=
PORT=
